/**
 * 练习6：在第一个包中创建至少有一个方法的接口。
 * 然后在第二个包中创建一个类，在其中增加一个protected的类以实现该接口。
 * 在第三个包中继承这个类，并在一个方法中返回该protected内部类的对象，
 * 在返回的时候向上转型为包中的接口的类型。
 * @author TyungLee
 *
 */
package cn.com.tyung.exercise_6;